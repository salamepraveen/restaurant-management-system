server:
  port: 8082

spring:
  application:
    name: AUTH_SERVICE # Recommended to use lowercase-kebab-case for service names

  datasource:
    url: jdbc:mysql://localhost:3306/pizza_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: praveen@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect # Good practice to specify the dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/